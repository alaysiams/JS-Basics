let name = "Linda";
let greeting = "Hi there";

// Create a function that logs out "Hi there, Linda!" when called

function welcome(name, greeting) {
  return `${greeting} ${name}`;
}

console.log(welcome("alice", greeting));
